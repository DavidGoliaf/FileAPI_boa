# M1 Specification Traceability Matrix

Public `BlobData` API is exactly the M1 contract (`empty`, `from_segments`, `size`,
`media_type`, `snapshot`, `segment_count`, `slice`) plus the M2 no-copy
composition primitives (`concat_shared`, `push_shared`). Raw segments, byte
reads, and source-identity probes are never public. Tests that need internals
(content of a blob, `Arc` sharing) live in the `#[cfg(test)]` module beside
`src/blob.rs` and read the private fields through normal child-module privacy;
integration suites live in `crates/boa_fapi_core/tests/`.

| ID | Normative rule | Code | Test |
|---|---|---|---|
| M1-CORE-01 | core does not depend on Boa | `crates/boa_fapi_core/Cargo.toml` — only `bytes` + `thiserror` | `guards_tests::core_cargo_toml_no_forbidden_dependencies` |
| M1-CORE-02 | ByteSource range/cancel contract | `crates/boa_fapi_core/src/source/mod.rs` — `ByteSource` trait; `crates/boa_fapi_core/src/source/memory.rs` — `MemorySource` impl | `source_tests::*` (19 tests: full/empty/prefix/middle/suffix range, start>end, end>len, u64::MAX, cancel before read, clone sees cancel, shares allocation) |
| M1-CORE-03 | immutable memory source | `crates/boa_fapi_core/src/source/memory.rs` — `MemorySource::new`, `read_range` returns `Bytes::slice` | `source_tests::memory_source_result_shares_allocation` (exact pointer equality via `Bytes::as_ptr()`), `blob::tests::memory_source_immutable_after_new`, `blob::tests::blob_data_immutable_repeated_reads` |
| M1-CORE-04 | Blob type normalization | `crates/boa_fapi_core/src/mime.rs` — `normalize_blob_type()` | `mime_tests::*` (16 tests: empty, ASCII lowercase, mixed case, spaces preserved, DEL/LF/CR/NUL/non-ASCII/emoji give empty, printable ASCII all preserved, property tests) |
| M1-CORE-05 | native line endings | `crates/boa_fapi_core/src/endings.rs` — `convert_line_endings_to_native()` | `endings_tests::*` (25 tests: both targets × empty/no-endings/CR/LF/CRLF/CRCR/LFLF/CRLFCR/start/end/Unicode, property: no standalone CR, count preserved) |
| M1-CORE-06 | segment validation and checked arithmetic | `crates/boa_fapi_core/src/blob.rs` — `BlobData::from_segments()` | `blob::tests::from_segments_*` (13 tests: single/multiple sources, offsets, zero-length filtered, zero-length invalid offset rejected, invalid offset/end, offset overflow, size at/over limit, count at/over limit, normalized MIME) |
| M1-CORE-07 | File API slice semantics | `crates/boa_fapi_core/src/blob.rs` — `BlobData::slice()` | `blob::tests::slice_*` (19 tests: None/None, 0/size, positive beyond, negative -1/-size/<-size, i64::MIN/MAX, end<start, empty, crosses 1/2/all segments, override type normal/invalid/none, original unchanged, `Arc::ptr_eq` sharing, fast path) + `blob::tests::slice_matches_reference` (property) |
| M1-CORE-08 | limits validation | `crates/boa_fapi_core/src/limits.rs` — `FileApiLimits::validate()` | `limits_tests::*` (17 tests: exact defaults, each zero limit, each invalid relation, boundary OK) |
| M1-CORE-09 | safe Rust/no panics in public paths | `crates/boa_fapi_core/src/lib.rs` — `#![deny(unsafe_code, clippy::unwrap_used, clippy::expect_used, clippy::panic)]`; root `Cargo.toml` `[workspace.lints.clippy]` denies `unwrap_used`, `expect_used`, `panic`, `todo`, `unimplemented` for every workspace member | `guards_tests::production_source_no_unwrap_expect_panic` (scanner strips `#[cfg(test)]` module bodies; scanner covered by `strip_test_modules_*` self-tests), `guards_tests::public_api_no_path_types` |

## M2 — Boa bindings (`crates/boa_fapi`)

| ID | Normative rule | Code | Test |
|---|---|---|---|
| M2-REG-01 | Atomic registration of `Blob`/`File` globals: build → preflight names/extensibility → install → rollback; never overwrite a host global | `crates/boa_fapi/src/extension.rs` — `FileApiExtension::register`, `install_globals`, `rollback_globals` | `m2_blob_file_filelist.rs::registration_installs_globals`, `::registration_name_conflict_rolls_back_atomically`, `::registration_fails_on_non_extensible_global`, `::registration_file_name_conflict_detected` |
| M2-REG-02 | Constructors have correct `name`, `length`, descriptors and prototype links; `File.prototype → Blob.prototype` | `extension.rs` — `build_blob_class`, `build_file_class` (ConstructorBuilder `name`/`length`/`inherit`); Web IDL descriptors in `blob.rs::init_prototype`, `file.rs::init_prototype` | `m2_blob_file_filelist.rs::constructor_descriptors_and_metadata`, `::prototype_links`, `::prototype_member_descriptors` |
| M2-REG-03 | `FileList` has no public constructor or global name | `extension.rs` — `build_file_list_prototype` (prototype only, nothing installed) | `m2_blob_file_filelist.rs::registration_installs_globals` (`typeof FileList === 'undefined'`), `::file_list_prototype_is_not_global` |
| M2-REG-04 | Chosen re-registration rule (b): every second `register` returns `AlreadyRegistered` | `extension.rs` — `register` (RegisteredSpecs marker in `Context` data) | `m2_blob_file_filelist.rs::registration_is_rejected_when_already_registered` |
| M2-WIDL-01 | One Web IDL conversion layer; no duplicated coercion in binding modules | `crates/boa_fapi/src/webidl.rs` — all converters and BlobPart/options conversion; binding modules call them | `webidl::tests::*` (unit tables); `guards.rs::internal_binding_modules_expose_no_public_items` |
| M2-WIDL-02 | `[Clamp] long long` for `slice`: NaN→0, infinities→bounds, nearest-ties-to-even rounding, clamp into i64; no `as` masking | `webidl.rs` — `clamped_long_long`, `f64_to_clamped_long_long` | `webidl::tests::clamp_*` (unit), `m2_blob_file_filelist.rs::slice_clamp_conversions` |
| M2-WIDL-03 | Separate exact converters for ordinary `long long` (lastModified) and `unsigned long` (item index); truncation + modulo semantics | `webidl.rs` — `long_long`, `unsigned_long`, `f64_to_long_long`, `f64_to_unsigned_long` | `webidl::tests::long_long_*`, `::unsigned_long_*` (unit); `m2_blob_file_filelist.rs::file_last_modified_supplied_conversion`, `::file_last_modified_ordinary_long_long_wrap`, `::file_list_length_conversion` |
| M2-WIDL-04 | `DOMString`/`USVString` conversions; BigInt/Symbol/throwing coercion → TypeError; getter exceptions propagate | `webidl.rs` — `usv_string`, `dom_string`, `parse_ending_mode`, `BlobOptions::parse`, `FileOptions::parse` | `m2_blob_file_filelist.rs::file_name_conversions`, `::bigint_last_modified_throws`, `::symbol_slice_args_throw`, `::throwing_options_getter_propagates` |
| M2-BLOB-01 | Blob parts left-to-right: USVString (UTF-8, `endings`), BufferSource, Blob/File; other part → synchronous TypeError, no partial Blob | `webidl.rs` — `blob_parts`, `collect_parts`, `process_part`, `PartsCollector` | `m2_blob_file_filelist.rs::string_parts_and_usv_replacement`, `::nested_blob_and_file_composition`; `src/tests.rs::string_parts_are_utf8_encoded`, `::native_endings_convert_bytes`, `::transparent_endings_preserve_bytes` |
| M2-BLOB-02 | BufferSource: exact copy of the visible view bytes at construction; detached buffer → empty byte sequence; later mutation invisible | `webidl.rs` — `array_buffer_bytes`, `view_bytes` | `src/tests.rs::buffer_source_copies_visible_range`, `::data_view_copies_visible_range`, `::post_construction_mutation_cannot_change_blob`, `::detached_buffer_copies_empty_sequence`; `m2_blob_file_filelist.rs::buffer_source_visible_ranges`, `::post_construction_mutation_is_invisible` |
| M2-BLOB-03 | Blob/File parts append shared immutable sources without payload copy; inner type ignored | `webidl.rs` — `PartsCollector::push_shared` → core `BlobData::push_shared`/`concat_shared` (no raw segment access) | `m2_blob_file_filelist.rs::nested_blob_and_file_composition` (JS-observable sizes); `src/tests.rs::nested_blob_composition_shares_source_without_copy`, `::nested_file_composition_shares_source_without_copy` (JS + M1 `size`/`segment_count` only); `blob::tests::concat_shared_*` (4 unit tests, `Arc::ptr_eq` via child-module private access) |
| M2-BLOB-04 | Options default `{type: "", endings: "transparent"}`; type → M1 normalize; only `transparent`/`native` valid | `webidl.rs` — `BlobOptions::parse`, `parse_ending_mode` | `m2_blob_file_filelist.rs::blob_type_normalization_and_readonly`, `::invalid_endings_throw`, `::empty_blob_defaults` |
| M2-BLOB-05 | `size`/`type` readonly getters; `Object.prototype.toString.call(blob)` = `[object Blob]` | `blob.rs` — `size_getter`, `type_getter`, `init_prototype` (toStringTag) | `m2_blob_file_filelist.rs::tostring_tags`, `::empty_blob_defaults`, `::blob_type_normalization_and_readonly` |
| M2-BLOB-06 | `slice` uses M1 `BlobData::slice`; absent contentType → empty type; result is a new Blob sharing sources | `blob.rs` — `slice` | `m2_blob_file_filelist.rs::slice_boundaries`, `::slice_content_type`, `::slice_result_is_new_blob_not_file`, `::sliced_source_stays_immutable`; `src/tests.rs::slice_shares_source_without_copy` |
| M2-FILE-01 | `new File(fileBits, fileName, options?)` with exact Blob parts/options algorithm | `file.rs` — `constructor` | `m2_blob_file_filelist.rs::file_type_and_endings`, `::file_requires_two_arguments` |
| M2-FILE-02 | `fileName` USVString then every `/` → `:`; no basename computation | `file.rs` — `normalize_file_name` | `m2_blob_file_filelist.rs::file_name_conversions`, `::host_file_from_bytes` |
| M2-FILE-03 | Absent `lastModified` reads the injected `Clock` | `extension.rs` — `RegisteredSpecs::now_unix_millis`; `file.rs` — `constructor`; `clock.rs` | `m2_blob_file_filelist.rs::file_last_modified_default_uses_clock`, `::host_file_from_bytes` |
| M2-FILE-04 | Supplied `lastModified` uses ordinary Web IDL `long long` | `webidl.rs` — `FileOptions::parse`, `long_long` | `m2_blob_file_filelist.rs::file_last_modified_supplied_conversion`, `::file_last_modified_ordinary_long_long_wrap`, `::bigint_last_modified_throws` |
| M2-FILE-05 | File is `instanceof File` and `instanceof Blob`; slice returns Blob; metadata readonly | `file.rs` — `FileNative`, getters; `extension.rs` — `build_file_class` inherit | `m2_blob_file_filelist.rs::real_file_passes_blob_brand`, `::subclassing_keeps_brand`, `::file_metadata_readonly`; `src/tests.rs::file_slice_is_a_plain_blob` |
| M2-FLIST-01 | Only `FileApiHandle::file_list` creates FileList; every element brand-validated before any output | `extension.rs` — `file_list`; `brand.rs` — `require_file_object`; `file_list.rs` — `create` | `m2_blob_file_filelist.rs::host_file_list_rejects_non_file_elements` |
| M2-FLIST-02 | Readonly `length`, `item(index)` (same File or `null`), indexed access (same File or `undefined`), host input order | `file_list.rs` — `length_getter`, `item`, `create` | `m2_blob_file_filelist.rs::file_list_order_identity_and_access`, `::file_list_length_conversion` |
| M2-FLIST-03 | Indexed own properties enumerable, readonly, non-replaceable | `file_list.rs` — `create` (writable:false, enumerable:true, configurable:false) | `m2_blob_file_filelist.rs::file_list_indexed_descriptors`, `::file_list_indexed_properties_are_readonly` |
| M2-FLIST-04 | `[object FileList]` toStringTag; brand gates on borrowed members | `file_list.rs` — `init_prototype`; `brand.rs` — `require_file_list` | `m2_blob_file_filelist.rs::file_list_brand_checks`, `::file_list_order_identity_and_access` |
| M2-GC-01 | Native data GC-safe: no `JsObject`/`JsValue`/`Context` in DTOs; non-GC fields marked via `boa_gc` derive without `unsafe` | `blob.rs`/`file.rs`/`file_list.rs` — native structs (`#[unsafe_ignore_trace]` on `Arc`/`String`) | `guards.rs::production_source_no_unwrap_expect_panic` (no `unsafe` tokens), all `src/tests.rs` execute under the real GC |
| M2-GC-02 | Public Rust/JS API exposes no native data, segments, brand keys or test hooks | `lib.rs` re-exports; private `mod` declarations; `brand.rs` private gates | `guards.rs::internal_binding_modules_expose_no_public_items`, `::lib_rs_denies_unsafe_and_limits_re_exports`, `::public_api_exposes_no_paths_or_mutable_bytes` |

## M3-A — Promise reads (`crates/boa_fapi`, `crates/boa_fapi_core`)

| ID | Normative rule | Code | Test |
|---|---|---|---|
| M3-READ-01 | `text`/`arrayBuffer`/`bytes` registered on `Blob.prototype` only (name/length/descriptors), inherited by `File`; brand violations are synchronous `TypeError` | `crates/boa_fapi/src/blob.rs` — `init_prototype` (M3 members); `crates/boa_fapi/src/promise_read.rs` — `text`, `arrayBuffer`, `bytes` via `require_blob` | `m3_promise_blob_reads.rs::read_methods_live_on_blob_prototype_with_correct_descriptors`, `::file_inherits_read_methods_without_own_copies`, `::brand_violations_throw_synchronously_without_promise` |
| M3-READ-02 | Pending `Promise` immediately; settlement only through Boa jobs (`run_jobs`); FIFO order | `promise_read.rs` — `read_promise` (`JsPromise::new_pending` + `PromiseJob` via `enqueue_job`) | `::text_returns_pending_promise_settled_by_run_jobs`, `::empty_blob_read_stays_pending_until_run_jobs`, `::two_concurrent_reads_settle_fifo` |
| M3-READ-03 | `text()` decodes UTF-8 with replacement (invalid/truncated → U+FFFD) | `promise_read.rs` — `package_bytes` (`String::from_utf8_lossy`) | `::text_decodes_ascii_and_multibyte`, `::text_replaces_invalid_utf8`, `::text_reads_composed_and_sliced_blobs` |
| M3-READ-04 | `arrayBuffer()` returns exact bytes in a fresh independent `ArrayBuffer` | `promise_read.rs` — `package_bytes` (`JsArrayBuffer::new` + copy) | `::array_buffer_returns_exact_bytes_in_fresh_buffer`, `::array_buffer_results_are_independent` |
| M3-READ-05 | `bytes()` returns fresh offset-0 `Uint8Array` over an independent buffer | `promise_read.rs` — `package_bytes` (`JsUint8Array::from_iter`) | `::bytes_returns_uint8array_with_offset_zero`, `::bytes_results_are_independent` |
| M3-READ-06 | Materialization limit rejects pending reads with the M4-A mapped `QuotaExceededError` DOMException (same-realm JS `instanceof` proof); boundary `size == limit` ok / `limit + 1` rejects; blob stays usable; non-limit core error rejects as the mapped `DOMException` (same-realm JS handler) | `boa_fapi_core/src/blob.rs` — `BlobData::materialize`; `crates/boa_fapi/src/promise_read.rs` — `reject_with` (M4-A central mapping) | `m3_promise_blob_reads.rs::over_materialize_limit_rejects_with_quota_exceeded`, `::materialize_limit_boundary`; `promise_read::tests::non_limit_error_rejects_with_mapped_dom_exception`, `::limit_error_rejects_with_quota_exceeded_dom_exception`, `::js_realm_verdict_helper_distinguishes_dom_exception`; core `blob::tests::materialize_*` (9 unit tests) |
| M3-READ-07 | `File` reads use exactly the Blob-brand path | `promise_read.rs` — `brand::require_blob` accepts `FileNative` | `::file_inherits_read_methods_without_own_copies`, `::text_decodes_ascii_and_multibyte` (File case) |
| M3-READ-08 | Bounded core materialize: multi-segment order, empty, exact/over limit, short/long source responses, cancel before/between, checked-offset failure | `boa_fapi_core/src/blob.rs` — `BlobData::materialize` | `blob::tests::materialize_multi_segment_order`, `::materialize_empty_blob`, `::materialize_exactly_at_limit`, `::materialize_one_over_limit_rejected_before_allocation`, `::materialize_rejects_short_source_response`, `::materialize_rejects_long_source_response`, `::materialize_cancelled_before_first_read`, `::materialize_cancelled_between_segments`, `::materialize_checked_offset_failure` |

## M3-B — Blob streams (`crates/boa_fapi`, `crates/boa_fapi_core`)

| ID | Normative rule | Code | Test |
|---|---|---|---|
| M3-STREAM-01 | Shim registration/atomicity: `ReadableStream` + `ReadableStreamDefaultReader` globals, exact descriptors/prototypes/`instanceof`, Blob-only `stream`/`textStream`, illegal construction/receivers `TypeError`, conflicts/non-extensible/feature-off leave no partial globals | `crates/boa_fapi/src/streams.rs` — `build_stream_specs`, `init_stream_prototype`, `init_reader_prototype`; `extension.rs` — `register`, `install_globals`, `rollback_globals` | `m3_blob_streams.rs::stream_surface_descriptors_and_inheritance`, `::shim_constructors_and_receivers_reject_synchronously`, `::full_streams_api_absent`, `::global_conflict_leaves_no_partial_streams`, `::streams_shim_disabled_fails_before_global_mutation`, `::non_extensible_global_rejects_streams_atomically` |
| M3-STREAM-02 | Bounded core reader: `validate()` + `reader()` enforce chunk size `16 KiB..=1 MiB`, `read_next()` yields one exact ordered chunk with O(chunk) memory | `boa_fapi_core/src/blob.rs` — `BlobData::reader`, `BlobReader::read_next`, `::cancel`; `limits.rs` — `validate`; `extension.rs` — `register` fail-fast | `limits_tests::chunk_size_below_minimum_rejected`, `::chunk_size_above_maximum_rejected`, `::chunk_size_bounds_accepted`; `blob::tests::reader_rejects_zero_and_out_of_range_chunk_size`, `::reader_delivers_exact_chunks_in_order`, `::reader_splits_across_segments_preserving_order`, `::reader_never_exceeds_chunk_ceiling`, `::reader_empty_blob_yields_done_immediately`; `m3_blob_streams::invalid_limits_reject_registration_before_any_global` |
| M3-STREAM-03 | Demand/FIFO/backpressure: nothing read before `read()`, one chunk per request, EOF repeats without reads; resolvers GC-safe in job captures | `streams.rs` — `reader_read`, `pump_one` (resolvers only in job closures) | `::reads_are_pending_until_run_jobs_with_fifo_order`, `::eof_repeats_without_source_reads`, `::empty_blob_resolves_done_first_read`, `::second_chunk_not_read_before_second_demand`; `streams::tests::pending_read_survives_gc_with_exact_chunk`, `::two_queued_reads_survive_gc_fifo_exact`, `::gc_then_cancel_and_error_paths_settle` |
| M3-STREAM-04 | Byte packaging: exact concatenation, fresh offset-0 `Uint8Array` with independent backing, composed/sliced/File | `streams.rs` — `package_bytes_chunk` | `::byte_chunks_concatenate_exactly_with_fresh_backing`, `::sixteen_kib_boundary_yields_exact_chunks`, `::composed_and_sliced_blobs_stream_in_order`, `::two_streams_are_independent` |
| M3-STREAM-05 | EOF semantics: `{value: undefined, done: true}`, repeated EOF without reads, empty first-done | `streams.rs` — `pump_one` (EOF + decoder flush) | `::eof_repeats_without_source_reads`, `::empty_blob_resolves_done_first_read` |
| M3-STREAM-06 | Cancel/release/isolation: idempotent cancels, locked-cancel rejection, queued-done, release rules, sibling independence | `streams.rs` — `stream_cancel`, `reader_cancel`, `release_lock`, `cancel_shared` | `::cancel_before_first_read_resolves_done`, `::locked_stream_cancel_rejects_with_type_error`, `::reader_cancel_makes_queued_and_future_reads_done`, `::release_lock_with_queued_read_throws_without_state_change`, `::release_lock_then_new_reader_works`, `::two_streams_are_independent` |
| M3-STREAM-07 | Errors/bounds: short/long/source failures reject with the M4-A mapped `DOMException` (same-realm, never plain `Error`/`RangeError`), terminal replay, chunk-size bounds | `blob.rs` — `BlobReader` terminal error class; `streams.rs` — `stream_error_reason` (M4-A central mapping) | `blob::tests::reader_terminal_error_replays_without_new_reads`, `::reader_propagates_source_error_as_same_class`; `::short_source_response_rejects_without_partial_chunk`, `::stream_core_error_rejects_plain_error_and_stays_terminal`, `::chunk_size_config_bounds_rejected`; `streams::tests::errored_stream_rejects_with_mapped_dom_exception`, `::errored_stream_future_reads_reject_without_new_reads` |
| M3-STREAM-08 | Incremental UTF-8 decoder: split multibyte never yields empty `done:false`, every-boundary exactness, invalid flush, no spurious chunks, independent decoders | `streams.rs` — `Utf8Decoder::push`, `::flush`, `incomplete_tail_len` (+ `pump_one` text coalescing loop) | `::text_stream_decodes_split_multibyte_without_early_replacement`, `::text_stream_split_at_every_boundary`, `::text_stream_invalid_flush_and_no_spurious_chunks` (16 KiB-1 + leader boundary case) |
| M3-STREAM-09 | File inheritance and no accidental API: File streams work, full Streams/M4-B absent (M4-A DOM/FileReader present) | `streams.rs` — `stream`/`text_stream` via `require_blob` | `::shim_constructors_and_receivers_reject_synchronously` (File case), `::composed_and_sliced_blobs_stream_in_order` (File), `::full_streams_api_absent`; `guards::streams_shim_surface_is_bounded`, `::filereader_and_dom_surface_is_bounded`, `::no_filereader_sync_or_out_of_scope_surface` |

## M4-A — DOM shim + async FileReader (`crates/boa_fapi`)

| ID | Normative rule | Code | Test |
|---|---|---|---|
| M4-DOM-01 | Feature `dom-shim` (default-on) + `dom_shim(bool)`; off fails with typed `DomShimDisabled` before any `globalThis` mutation | `crates/boa_fapi/src/extension.rs` — `register`, `install_globals`, `rollback_globals`; `error.rs` — `RegisterError::DomShimDisabled` | `m4_filereader_async.rs::dom_shim_disabled_fails_before_global_mutation`, `::non_extensible_global_rejects_dom_atomically`, `::each_new_global_conflicts_atomically` |
| M4-DOM-02 | Exact globals/descriptors/prototypes/brands: `EventTarget`, `Event`, `ProgressEvent`, `DOMException`, `FileReader` as writable/non-enumerable/configurable; constructor `name`/`length`; `FileReader.prototype → EventTarget.prototype`, `ProgressEvent.prototype → Event.prototype`, `DOMException.prototype → Error.prototype` | `dom.rs` — `build_dom_specs`, `init_*_prototype`; `filereader.rs` — `build_filereader_specs` | `::dom_globals_have_exact_descriptors_and_prototypes`, `::dom_constructors_have_exact_name_and_length`, `::filereader_surface_descriptors_and_initial_state`, `::filereader_constants_are_readonly` |
| M4-DOM-03 | `EventTarget` listener tuple dedupe/removal/order, forged-receiver `TypeError`, exception isolation (first error after all listeners) | `dom.rs` — `add_event_listener`, `remove_event_listener`, `dispatch_event`, `invoke_event` | `::event_target_listener_dedupe_removal_order_and_isolation`, `::illegal_receivers_throw_type_error_synchronously` |
| M4-DOM-04 | `Event`/`ProgressEvent` readonly attributes, `target`/`currentTarget` identity, flags, `preventDefault`/`stopImmediatePropagation`, `dispatchEvent` return | `dom.rs` — event getters, `event_prevent_default`, `event_stop_immediate` | `::event_and_progress_event_attributes_are_exact` |
| M4-DOM-05 | `DOMException` same-realm `instanceof`, required names, `Error` inheritance, `[object DOMException]`; central `map_core_error` mapping | `dom.rs` — `dom_exception_constructor`, `map_core_error`, `construct_exception` | `::dom_exception_names_and_error_inheritance`, `promise_read::tests::*`, `streams::tests::*` |
| M4-FR-01 | `FileReader` surface: constructor/`new`-only, 5 methods (`length` 1/1/1/1/0), readonly attributes, 6 `on*` handlers, constants on constructor+prototype, initial `(EMPTY, null, null)` | `filereader.rs` — `filereader_constructor`, `init_filereader_prototype`, `init_filereader_constants` | `::filereader_surface_descriptors_and_initial_state`, `::filereader_constants_are_readonly`, `::filereader_construction_and_receiver_brand_checks` |
| M4-FR-02 | `readAsArrayBuffer`: exact bytes, empty, NUL/high-byte, composed/sliced/File, fresh independent backing | `filereader.rs` — `read_as_array_buffer`, `finish_at_eof` | `::read_as_array_buffer_is_exact_and_fresh` |
| M4-FR-03 | `readAsBinaryString`: one code unit per byte, embedded NULs | `filereader.rs` — `read_as_binary_string` | `::read_as_binary_string_preserves_nuls_and_high_bytes` |
| M4-FR-04 | `readAsText`: default UTF-8, BOM, replacement, split 2/3/4-byte boundaries, non-UTF-8 labels via `encoding_rs`, unknown label → `EncodingError` | `filereader.rs` — `read_as_text`, `IncrementalDecoder`, `resolve_label` | `::read_as_text_utf8_bom_replacement_and_split_boundaries` |
| M4-FR-05 | `readAsDataURL`: exact `data:<type>;base64,<payload>` / `data:;base64,`, no whitespace, checked arithmetic vs `max_data_url_output` (`==limit` ok / `+1` fails) | `filereader.rs` — `read_as_data_url`, `finish_at_eof` | `::read_as_data_url_exact_packaging`, `::data_url_quota_boundary` |
| M4-FR-06 | State/event model: `loadstart`, throttled `progress`, final progress, `load`, `loadend`; per-event `target`/`currentTarget`/flags/fields; 50 ms throttle on injected `Clock` (one pump = one clock sample) | `filereader.rs` — `run_pump`, `run_dispatch`, `dispatch_event_now` | `::empty_blob_full_event_sequence_is_exact`, `::multichunk_sequence_has_final_progress_before_load`, `::progress_throttle_uses_injected_clock` |
| M4-FR-07 | Sync LOADING guard (`InvalidStateError`, state preserved) + reentrancy (new read in `load`/`error`/`abort` suppresses only old `loadend`) | `filereader.rs` — `start_read`, `run_dispatch` | `::second_read_while_loading_throws_invalid_state`, `::reentrant_load_starts_new_read_and_suppresses_old_loadend`, `::reentrant_error_handler_starts_new_read`, `::abort_handler_restart_after_mid_chunk_abort` |
| M4-FR-08 | Abort races: before first job, between chunks, EMPTY/DONE silence, stale completion no-op, GC-safe (`force_collect`), quota return; stale jobs never read/mutate/release/emit after `loadstart`/`progress`/final-progress reentrancy | `filereader.rs` — `abort`, generation guards, `QueueHolder` | `::abort_before_first_job_emits_only_abort_loadend`, `::abort_between_chunks_suppresses_stale_events`, `::abort_in_empty_or_done_state_is_silent`, `::stale_completion_after_new_operation_is_noop`, `::gc_survives_queued_filereader_jobs`; `filereader::tests::loadstart_abort_performs_no_source_read`, `::loadstart_abort_then_restart_emits_only_new_operation`, `::progress_abort_freezes_source_reads` |
| M4-FR-09 | Failure/quota/limits: `NotReadableError` mapping (short/long/source), `EncodingError`, Data-URL boundary, 64-ok/65th-`SecurityError`, slot recovery | `filereader.rs` — `fail_operation`, `fail_fast`, `release_slot` | `::data_url_quota_boundary`, `::concurrent_read_quota_recovers_after_success_error_abort`, `::read_as_text_utf8_bom_replacement_and_split_boundaries` (EncodingError); `filereader::tests::short_source_response_fails_as_not_readable_error`, `::long_source_response_fails_as_not_readable_error`, `::failing_source_fails_as_not_readable_error`, `::error_handler_restart_suppresses_old_loadend` |
| M4-FR-10 | M3 regression (DOMException mapping, unchanged success), property/model sequences (162-scenario enumerated corpus vs pure model), negative guards (no `FileReaderSync`/fs/URL/clone/WPT/full-DOM) | `promise_read.rs` — `reject_with`; `streams.rs` — `stream_error_reason`; guards | `::m3_promise_rejections_are_dom_exceptions_with_fixed_mapping`, `::bounded_operation_sequences_match_pure_model`, `::excluded_m4b_apis_are_absent`; `guards::filereader_and_dom_surface_is_bounded`, `::no_out_of_scope_surface`, `::sync_surface_is_bounded` |

## M4-B - FileReaderSync (crates/boa_fapi)

| ID | Normative rule | Code | Test |
|---|---|---|---|
| M4B-FRS-01 | Environment capability: default Window installs no sync global; DedicatedWorker/SharedWorker install exactly FileReaderSync; ServiceWorker installs none; descriptor is explicit host config, atomic preflight/rollback, name conflicts fail fast | `crates/boa_fapi/src/extension.rs` — FileApiEnvironment, FileApiExtensionBuilder::environment, register, install_globals, rollback_globals | `m4_filereader_sync.rs::window_has_no_sync_global`, `::service_worker_has_no_sync_global`, `::dedicated_worker_installs_sync_global`, `::shared_worker_installs_sync_global`, `::default_environment_is_window_without_sync`, `::sync_name_conflict_fails_atomically`, `::window_leaves_foreign_sync_name_untouched` |
| M4B-FRS-02 | Exact surface: constructor new-only with name/length/descriptors and Symbol.toStringTag; prototype holds only the four methods (length 1/1/1/1); no readyState/result/error/abort/handlers/Promise API; brand and argument TypeErrors | `filereader_sync.rs` — build_sync_specs, init_sync_prototype, require_sync | `::sync_surface_descriptors_and_tags`, `::sync_prototype_has_only_four_methods`, `::sync_construction_and_receiver_brand_checks` |
| M4B-FRS-03 | All four methods on empty Blob, NUL/high-byte data, composed/sliced Blob and File; fresh independent ArrayBuffer | `filereader_sync.rs` via `package.rs` packagers | `::sync_array_buffer_is_exact_and_fresh`, `::sync_binary_string_preserves_nuls_and_high_bytes` |
| M4B-FRS-04 | Text parity with async: default UTF-8, single leading BOM removal, replacement, multibyte, supported labels; unknown label throws EncodingError; throwing labels convert after brand/argument checks | `filereader_sync.rs` — read_as_text_sync via `package::decode_text`/`resolve_label` | `::sync_text_matches_async_representations` (incl. `EncodingError` throw), `::throwing_label_is_converted_after_brand_and_argument_checks` |
| M4B-FRS-05 | Exact data URL with empty/non-empty MIME; boundary max_data_url_output (== ok / +1 throws QuotaExceededError) before reads/allocation | `filereader_sync.rs` — read_as_data_url_sync via `package::data_url_len`/`package_data_url` | `::sync_data_url_exact_packaging`, `::sync_data_url_quota_boundary` |
| M4B-FRS-06 | max_sync_read_bytes boundary (== ok / +1 throws QuotaExceededError for every method); rejected preflight performs zero source reads | `filereader_sync.rs` — read_bytes_sync preflight | `::sync_size_limit_boundary`; `filereader_sync::tests::rejected_sync_preflight_performs_zero_source_reads` |
| M4B-FRS-07 | Source failures (short/long/explicit) throw same-realm NotReadableError with no partial result and no process panic; shared packaging boundary with async | `filereader_sync.rs` — throw_mapped; `package.rs` | `filereader_sync::tests::short_source_throws_not_readable_error`, `::long_source_throws_not_readable_error`, `::failing_source_throws_not_readable_error`, `::sync_success_paths_match_async_packaging` |
| M4B-FRS-08 | Sync/async isolation: sync returns without jobs/events; 64 async slots usable alongside sync reads; async quota intact; powerset green; negative guards | `extension.rs` capability wiring; guards | `::sync_methods_return_without_jobs_or_events`, `::sync_reads_do_not_consume_async_quota`, `::window_and_service_worker_never_expose_sync`, `::no_filesystem_url_clone_or_full_dom_surface`; `guards::sync_surface_is_bounded`, `::no_out_of_scope_surface`; full M4-A suite green |

## M5-FS — filesystem-backed File, capability policy, snapshot security

| ID | Normative rule | Code | Test |
|---|---|---|---|
| M5-FS-01 | Capability-only host import, no raw JS location | `crates/boa_fapi_fs/src/capability.rs` — `FsRegistry`/`RegisteredResource` (opaque id only; `close` removes the slot, `close_all` drops all handles, `on_shutdown`/`run_closers` for runtime shutdown); `crates/boa_fapi/src/extension.rs` — `file_from_resource(registry, Arc<dyn FileResource>, ...)` (validates + weak-platform gate before any JS object; tracks the registry for shutdown) | `fs_tests::close_removes_slot_and_drops_handle`, `::close_all_drops_every_handle`, `::shutdown_closers_run_once_outside_lock`, `m5_file_fs.rs::file_list_accepts_only_explicit_files`, `::display_name_is_the_only_visible_name`; `cargo test --package boa_fapi_fs --all-features` |
| M5-FS-02 | Opaque resource identity, enforced copy-or-deny off-Unix | `crates/boa_fapi_fs/src/identity.rs` — `platform_has_strong_identity() == cfg!(unix)`, `capture`/`snapshot_of` (Unix dev+ino only; no weak fallback); `crates/boa_fapi_fs/src/source.rs` — `FileSource::new` refuses off-Unix, `new_for_copy` backs `open_copy_on_import`; `crates/boa_fapi_fs/src/policy.rs` — `RegistryPolicy::authorize_open` refuses off-Unix; `extension.rs` — `file_from_resource` weak-platform gate | `fs_tests::unix_permissions_and_symlink_escape` (cfg unix), `::weak_platform_direct_import_is_refused` (all platforms, asserts refusal off-Unix), `m5_file_fs.rs::weak_platform_copy_reports_no_location_detail` (cfg not unix) |
| M5-FS-03 | Snapshot validation before first/new chunk (Unix live path) | `crates/boa_fapi_fs/src/source.rs` — `FileSource::read_range` (pre-read + post-read checks); `crates/boa_fapi/src/extension.rs` — `ArcResourceSource::read_range` (same order + shutdown); weak platforms use point-in-time copies (no live revalidation by design) | `fs_tests::truncate_is_detected_before_new_chunk`, `::replacement_is_detected`, `::delete_is_detected` (cfg unix), `m5_file_fs.rs::changed_file_read_fails_not_readable_without_partial`, `::changed_file_filereader_fails_not_readable` (cfg unix), `::blob_size_preflight_boundary` (stale/weak-identity import denial) |
| M5-FS-04 | Permissions/lock/short-read mapping; no I/O under the global lock | `crates/boa_fapi_fs/src/capability.rs` — `map_io`, `cloned_handle` (`try_clone` under a short lock, all I/O after unlock; Unix positional / non-Unix independent cursor); `source.rs` — short/over-long → `InvalidRange`; central `dom::map_core_error` (unchanged, no second mapping) | `fs_tests::short_read_never_returns_partial`, `::oversized_range_rejected`, `::unix_permissions_and_symlink_escape` (cfg unix), `m5_file_fs.rs::changed_file_*` (cfg unix DOMException assertions) |
| M5-FS-05 | No location/identity disclosure | No location in public types/methods/errors; fixed generic messages | `fs_tests::no_location_in_public_types_or_errors`, `m5_file_fs.rs::js_errors_carry_no_location_detail` (cfg unix), `::weak_platform_copy_reports_no_location_detail` (cfg not unix), `::display_name_is_the_only_visible_name`; `guards.rs::public_api_exposes_no_paths_or_mutable_bytes`, core `guards_tests::public_api_no_path_types` |
| M5-FS-06 | Limits and no partial result; copy closes handle on every exit | `crates/boa_fapi/src/blob.rs` — `data_from_fs_source` (preflight `max_blob_size`); `crates/boa_fapi_fs/src/source.rs` — `open_copy_on_import` (closes the live handle on success, limit refusal, allocation failure, and read error; each copy consumes its registration); shared checked materialization/reader/stream/sync paths (unchanged mapping, no bypass) | `fs_tests::empty_file_exact_and_boundary_ranges`, `::exact_ranges_and_checked_arithmetic` (cfg unix), `::copy_bounds_and_content_everywhere` (== ok / +1 `ResourceLimit` on a fresh slot + handle released), `::copy_on_import_bounds_and_content`, `::failed_copy_releases_handle_on_every_path`, `m5_file_fs.rs::blob_size_preflight_boundary`, `::sync_limit_preflight_for_fs_source`, `::materialize_limit_rejects_fs_promise_read`, `::file_from_resource_stream_and_slice` |
| M5-FS-07 | Feature-safe atomic registration | `crates/boa_fapi/Cargo.toml` — `fs` feature (default on, `dep:boa_fapi_fs`); `fs`-gated `file_from_resource`/shutdown/types; existing preflight/rollback unchanged | `cargo hack check --feature-powerset --depth 2`; `m5_file_fs.rs` (cfg `fs`); no-partial-global contract (existing M2/M4 suites green with `fs` on/off) |
| M5-FS-08 | Shutdown drops handles, cancels work, no late callbacks | `crates/boa_fapi/src/lifecycle.rs` — `ShutdownFlag` (closed bit + cancellation + tracked closers, exactly-once); `FileApiHandle::shutdown` runs `close_all` per tracked registry; closed-state checks in `settle_read`, FileReader pump/dispatch, `pump_one`, `ArcResourceSource`, host-handle rejects | `fs_tests::close_all_drops_every_handle`, `::shutdown_closers_run_once_outside_lock`, `m5_file_fs.rs::shutdown_releases_live_handles` (cfg unix), `::shutdown_releases_handles_and_rejects_late_use`, `::shutdown_rejects_new_operations_and_repeats_idempotently`, `::shutdown_before_jobs_settles_nothing_late`, `::post_shutdown_source_reads_fail` |
| M5-FS-09 | Memory-backed M2–M4 regression | Unchanged memory semantics (`MemorySource`, `BlobData` memory paths, all readers) | `cargo test --package boa_fapi --test m2_blob_file_filelist`, `--test m3_promise_blob_reads`, `--test m3_blob_streams`, `--test m4_filereader_async`, `--test m4_filereader_sync` (all green) |
| M5-FS-10 | CI parity and reproducible handoff | `.github/workflows/ci.yml` — `boa_fapi_fs` tests + M5 host integration on Ubuntu and Windows | `cargo test --package boa_fapi_fs --all-features`, `cargo test --package boa_fapi --test m5_file_fs`; `docs/reviews/M5-handoff.md` |

## M6-URL — Blob URL store, resolver, JS surface, environments (`boa_fapi_core`, `boa_fapi`)

| ID | Normative rule | Code | Test |
|---|---|---|---|
| M6-URL-01 | CSPRNG URL format, no secret internals, no overwrite | `crates/boa_fapi_core/src/blob_url.rs` — `format_uuid_v4` (v4 bits), `format_blob_url` (`blob:<origin>/<uuid>`), `parse_blob_url` (strict shape), `BlobUrlError::Malformed/Unavailable` (identical display, no token/UUID/origin/existence/host detail); `crates/boa_fapi/src/extension.rs` — `OsEntropy` (`getrandom`), `create_url_for_specs` (bounded collision retry, never overwrite) | `blob_url.rs::uuid_v4_format_and_bits`, `::url_format_and_parse_round_trip`, `::url_failures_share_one_opaque_class`, `::url_collision_never_overwrites`; `m6_blob_url.rs::generated_url_format_and_no_secret_internals`, `::stuck_entropy_collides_without_overwrite`, `::zero_entropy_fails_without_minting` |
| M6-URL-02 | same-origin + same-partition isolation | `blob_url.rs` — `EnvironmentDescriptor`/`EnvironmentKey` (origin + partition + nonce; both `Debug` redacted), `BlobUrlStore::resolve` (full-key check before `Arc`); `extension.rs` — per-context store/key, no public store/key escape (`blob_url_count`/`blob_urls_empty` only) | `blob_url.rs::url_partition_and_nonce_isolation`, `::url_descriptor_debug_redacts_partition`, `::url_key_debug_redacts_partition_and_nonce`, `::url_revoke_is_ownership_blind`; `m6_blob_url.rs::same_origin_partitions_isolate` |
| M6-URL-03 | create/revoke semantics | `crates/boa_fapi/src/url_shim.rs` — `create_object_url` (Blob-brand only; FileList/forged/MediaSource → `TypeError`), `revoke_object_url` (required-arg + central `webidl::dom_string`, abrupt conversion propagates; converted strings revoke silently, always `undefined`); `extension.rs` — `create_blob_url`/`revoke_blob_url` (shared helper); store `revoke` keeps handed-out `Arc` alive | `m6_blob_url.rs::create_revoke_semantics`, `::revoke_webidl_conversion`, `::url_surface_descriptors_and_brand`; `blob_url.rs::url_revoke_keeps_live_reads_and_clear_releases` |
| M6-URL-04 | limits and atomic registration | `extension.rs` — `insert_capped` quota atomicity (`max_blob_urls_per_global`, `0` fails via `validate()`), URL name preflight + `rollback_globals` | `m6_blob_url.rs::url_limits_and_atomic_registration`; `blob_url.rs::url_quota_is_atomic` |
| M6-URL-05 | environment gating | `extension.rs` — `FileApiEnvironment::core_kind`, `creation_forbidden` (ServiceWorker), `url_shim` flag + `url-shim` feature (off → no `URL` global, M1–M5 intact) | `m6_blob_url.rs::url_environment_gating` |
| M6-URL-06 | shutdown lifetime | `lifecycle.rs` + `extension.rs` — tracked `store.clear()` closer, idempotent `shutdown`, closed-state checks in `create_url_for_specs`/handle rejects; late jobs settle nothing; shutdown release proved via `blob_url_count`/`blob_urls_empty` (no store escape) | `m6_blob_url.rs::url_shutdown_lifetime`, `::url_name_conflict_rolls_back_atomically`; `blob_url.rs::url_revoke_keeps_live_reads_and_clear_releases` |

## M6-CLONE — structured-clone bridge (`boa_fapi_core`, `boa_fapi`, no `boa-idb`)

| ID | Normative rule | Code | Test |
|---|---|---|---|
| M6-CLONE-01 | Blob/File payload round-trip | `crates/boa_fapi_core/src/clone.rs` — `serialized_blob`/`serialized_file` (M1 MIME normalize, sanitized name, stored `lastModified`); `extension.rs` — `clone_blob`/`clone_file` (checked `materialize`), `blob_from_clone`/`file_from_clone` (new immutable backing) | `blob_url.rs::clone_round_trips_preserve_bytes_and_metadata`; `m6_structured_clone.rs::clone_blob_file_round_trip` |
| M6-CLONE-02 | FileList round-trip | `extension.rs` — `clone_file_list` (brand every element before output), `file_list_from_clone` (checked per-file decode, no partial list) | `m6_structured_clone.rs::clone_file_list_round_trip`; `blob_url.rs::clone_round_trips_preserve_bytes_and_metadata` (list case) |
| M6-CLONE-03 | filesystem safety | `extension.rs` — clone through the checked materialization path; `SourceFailed` typed, no partial payload, no path/capability in errors | `m6_structured_clone.rs::clone_filesystem_safety_unix` (cfg unix live revalidation) |
| M6-CLONE-04 | versioned checked encoding | `clone.rs` — `FCL1`/v1/`SCF_*` layout, `validate_payload` pre-emit bounds (symmetric with decode: bytes/strings/count/total, checked arithmetic), `Cursor` checked decode, `Malformed`/`UnsupportedVersion`/`LimitExceeded`, same-version fixture | `blob_url.rs::clone_decode_rejects_bad_input`, `::clone_encode_bounds_are_symmetric`, `::clone_same_version_fixture`; `m6_structured_clone.rs::clone_versioned_checked_encoding`, `::clone_host_path_enforces_blob_media_type_bound` |
| M6-CLONE-05 | adapter/lifecycle | `extension.rs` — `CloneAdapter`/`CloneBridgeDescriptor` version preflight (`CloneBridgeIncompatible` + rollback), `NoBridge`/`Shutdown` before payload; `clone_bridge.rs` — host bridge + fake reference | `m6_structured_clone.rs::clone_adapter_lifecycle`, `::clone_feature_off_keeps_m1_m5` |

## M6-REG — regression

| ID | Normative rule | Code | Test |
|---|---|---|---|
| M6-REG-01 | memory and M5 regression | Unchanged M1–M5 semantics (brands, prototypes, descriptors, conversions, readers, streams, snapshot, limits, shutdown, central mapping); M4 negative guards updated for the normative M6 `URL` surface; powerset green | `cargo test --workspace --all-features` (42 boa_fapi unit, 13 guards, 51 M2, 28 M3-B, 16 M3-A, 33 M4-A, 21 M4-B, 15 M5-JS, 58+14+25+8+20+16+19 core incl. 14 M6-core, 9 fs, 11+6 M6-JS, 1 doc); `cargo hack check --feature-powerset --depth 2`; `guards::no_out_of_scope_surface`, `::public_api_exposes_no_paths_or_mutable_bytes` (no `boa-idb`, bounded URL surface, no store/key escape) |

## M6-RW — rework findings R1–R4 (`docs/reviews/M6-rework.md`)

| ID | Fixed boundary | Code | Test |
|---|---|---|---|
| M6-RW-R1 | no public mutable store/key escape | `extension.rs` — removed `FileApiHandle::url_store`/`environment_key`; added count-only `blob_url_count`/`blob_urls_empty`; `resolve_blob_url` builds the key internally; `BlobUrlStore::insert_capped` documented as test-only (no handle forwards to it); shutdown/ServiceWorker/quota stay in the single `create_url_for_specs` path | `m6_blob_url.rs::url_shutdown_lifetime`, `::zero_entropy_fails_without_minting` (count-only assertions); `guards::public_api_exposes_no_paths_or_mutable_bytes` (forbids `pub fn url_store`, `pub fn environment_key`, `SharedUrlStore`/`EnvironmentKey` returns) |
| M6-RW-R2 | required-arg/DOMString revoke | `url_shim.rs` — `revoke_object_url` via central `webidl::dom_string`; missing arg throws `TypeError`; abrupt conversion propagates; converted strings revoke silently | `m6_blob_url.rs::revoke_webidl_conversion`, `::create_revoke_semantics` (missing-arg case) |
| M6-RW-R3 | symmetric clone encode bounds | `clone.rs` — `serialized_blob` enforces `MAX_CLONE_STRING_BYTES`; `encode` runs `validate_payload` (bytes/strings/count/total, checked) before any byte; format unchanged | `blob_url.rs::clone_encode_bounds_are_symmetric`; `m6_structured_clone.rs::clone_host_path_enforces_blob_media_type_bound` |
| M6-RW-R4 | redacted key identity | `blob_url.rs` — manual redacted `Debug` for `EnvironmentKey` (origin visible; partition/nonce `<redacted>`); `Eq`/`Hash` unchanged | `blob_url.rs::url_key_debug_redacts_partition_and_nonce` |

## M7-WPT — deterministic harness over the pinned adapted subset (`boa_fapi_wpt`)

| ID | Normative rule | Code | Test |
|---|---|---|---|
| M7-WPT-01 | pinned corpus, SHA-256 and manifest completeness | `crates/boa_fapi_wpt/src/manifest.rs` — `load_manifest` (schema 1, pinned commit hex, per-file SHA-256, exact subtests, dup-path rejection, depth/array/number/string bounds); `src/main.rs` — `verify_hashes` before execution, mismatch is a launch error | `manifest::tests::accepts_pass_manifest`, `::rejects_bad_schema_and_bad_hash`, `::rejects_deep_nesting_surrogate_and_dup_path`; strict CLI run on `wpt-manifest.json` (7 files / 38 subtests, all PASS) |
| M7-WPT-02 | `.any.js`/JS-only adaptation in a real Boa Context | `src/harness.rs` — `prelude_source` (no upstream fetch/DOM); `src/runner.rs` — `run_file` (fresh `Context` + `FileApiExtension` per file); `corpus/*.js` adapted subsets | `blob-constructor` (11), `blob-slice` (5), `file-constructor` (5), `filelist-section` (2), `bloburl-create-revoke` (6) subtests via `--strict` |
| M7-WPT-03 | assertions, async/promise tests, job pump and timeout | `src/harness.rs` — `test`/`async_test`/`promise_test`, assertions, `done`/`step`; `src/runner.rs` — bounded `run_jobs()` pump (64 passes + wall guard), `TIMEOUT` vs JS-failure split | `reading-data-section` (5, `promise_test`), `filereader-read` (4), `runner::tests::passing_file_maps_to_pass`, `::failing_assertion_maps_to_fail` |
| M7-WPT-04 | deterministic statuses and JSON/JUnit output | `src/report.rs` — `to_json`/`to_junit` (manifest order, fixed keys, no timestamps/paths/secrets; `scrub_detail` redacts `blob:`) | `report::tests::strict_gate_needs_exact_match`, `::json_and_junit_escape_deterministically`; `--json`/`--junit` strict artifacts in CI |
| M7-WPT-05 | exact expectations and strict unexpected-result gate | `src/manifest.rs` — exact entries, no wildcards, `MissingReason`/expiry load errors; `src/report.rs` — `strict_pass` (unexpected rows break strict) | `manifest::tests::rejects_unknown_status_and_wildcards`, `::rejects_missing_reason_and_expired_review`; strict CLI exit non-zero on any mismatch |

## M7-RACE — deterministic race matrix (`boa_fapi`)

| ID | Normative rule | Code | Test |
|---|---|---|---|
| M7-RACE-01 | FileReader abort/stale completion/quota races | `crates/boa_fapi/tests/abort_races.rs` — abort before first chunk, abort between progress events, stale-after-restart, 64+1 quota with recovery | `::abort_before_first_chunk_reports_abort_loadend`, `::abort_between_progress_events_suppresses_stale_load`, `::stale_completion_after_new_operation_is_noop`, `::concurrent_read_quota_recovers` |
| M7-RACE-02 | URL revoke/resolve and context shutdown races | `abort_races.rs` — revoke-before/after-resolve with live Arc reads, shutdown with pending promise/reader (no late settlement, late creation rejected) | `::revoke_before_and_after_resolve`, `::context_shutdown_settles_nothing_late` |
| M7-RACE-03 | filesystem snapshot mutation and UTF-8 boundary matrix | `abort_races_fs.rs` — post-creation mutation → mapped `DOMException`, never partial bytes (Unix live / non-Unix copy); `abort_races.rs` — `i64::MIN/MAX`/empty/reversed slices, multibyte sizes, BOM strip | `::filesystem_mutation_after_creation`, `::slice_and_utf8_boundary_matrix`, `::worker_environment_sync_without_jobs` |

## M7-HARD — leak/repeat, differential/fuzz/bench, CI/nightly (`boa_fapi`, `boa_fapi_wpt`)

| ID | Normative rule | Code | Test |
|---|---|---|---|
| M7-HARD-01 | leak/repeat tests and no late JS settlement | `crates/boa_fapi/tests/hardening_hooks.rs` — `leak_repeat` (8× create/revoke/shutdown cycles, counts to zero, fresh contexts isolated) | `hardening_hooks::leak_repeat` |
| M7-HARD-02 | bounded differential/fuzz/bench hooks | `hardening_hooks.rs` — `differential_smoke` (Node when present else skip-with-reason), `fuzz_decode_bounded` (fixed seed, 64 KiB cap, 512 cases), `bench_smoke` (clone/resolve medians, informational) | `hardening_hooks::differential_smoke`, `::fuzz_decode_bounded`, `::bench_smoke` |
| M7-HARD-03 | CI matrix, SBOM artifact and scheduled nightly checks | `.github/workflows/ci.yml` (M7 steps + SBOM artifact), `.github/workflows/nightly.yml` (scheduled fuzz/miri-shape runs, visible failures) | CI Ubuntu + Windows strict run; nightly schedule |

## M7-DOC — documentation

| ID | Normative rule | Code | Test |
|---|---|---|---|
| M7-DOC-01 | WPT/spec-delta/README/changelog documentation | `docs/wpt.md`, `docs/spec-delta.md`, `README.md` (M7 scope), `CHANGELOG.md`, `QUESTIONS.md` (supply decision), `docs/DECISIONS.md` (ADR-0033 harness deps) | `cargo doc --workspace --no-deps` (`-Dwarnings`); reproduced strict run from docs alone |

## M7-RW — rework findings F1–F11 (`docs/reviews/M7-rework.md`)

| ID | Fixed boundary | Code | Test |
|---|---|---|---|
| M7-REWORK-F1 | strict+filter launch error | `main.rs` — `run` rejects `--strict` with `--filter` before manifest read | boundary: `--strict --filter` → exit 2 `--filter cannot be combined with --strict` |
| M7-REWORK-F2 | corpus_root + strict path scheme | `manifest.rs` — `corpus_root` field, `check_logical_path`/`check_corpus_root`/`resolve_corpus_path` (Path join, canonicalize, strict containment, symlink rejection); `main.rs` — `verify_hashes` over raw bytes | boundary: `../`, absolute, backslash, NUL, non-`.js`, symlink, outside-root → launch error exit 2; only logical `file.path` in details |
| M7-REWORK-F3 | actual NOTRUN end to end | `runner.rs` — `ActualStatus::NotRun`; `report.rs` — enum-compared `strict_pass`, JSON `NOTRUN`, JUnit `<skipped>`; `main.rs` — separate summary count | boundary: expected NOTRUN → JSON/JUnit actual NOTRUN + skipped; `report::tests::strict_gate_needs_exact_match` (all four statuses) |
| M7-REWORK-F4 | async/job errors are FAIL | `harness.rs` — `record_once`/`safe_error`/`run_step`, step-wrapped `test`/`async_test`/`promise_test` (cleanup/double-done/throwing-callback FAIL); `runner.rs` — job-error flag, top-level throw fails every row | boundary: async assertion throw → FAIL (never TIMEOUT); `runner::tests` + strict corpus prove it |
| M7-REWORK-F5 | process isolation + wall kill | `main.rs` — `--worker-file <index>` (validated ID only), `current_exe` spawn, bounded stdout/stderr, `Child::kill` at deadline → `TIMEOUT`, `run_file` kept as library mapping | boundary: self-scheduling file → TIMEOUT, parent continues next file; worker exit/output corruption → TIMEOUT |
| M7-REWORK-F6 | threads N with ordered output | `main.rs` — `run_files_parallel` (min(N, files) slots, index-chunked isolated workers, `BTreeMap` re-sort); no `Context` crosses threads | boundary: `--strict --threads 2` exit 0 with SHA-256-identical JSON to `--threads 1` |
| M7-REWORK-F7 | failure mapping without masking | `runner.rs`/`main.rs`/`report.rs` — launch error (manifest/path/hash/parser, prelude, readback) vs FAIL rows (top-level throw poisons file, job error poisons rows) vs TIMEOUT (pump budget, kill, overflow) vs NOTRUN (clean gap) | `strict_pass` enum comparison; empty results impossible (missing rows are TIMEOUT/FAIL, never skipped) |
| M7-REWORK-F8 | manifest completeness + schema hardening | `manifest.rs` — required `corpus_root`, HTTPS WPT repository, group allow-list + all-six-groups gate, full per-subtest records, real calendar `review_by`, duplicate-key rejection, path/name/meta/corpus/timeout limits | `manifest::tests::*` (schema/hash/status/wildcard/reason/expiry/depth/surrogate/dup-path) |
| M7-REWORK-F9 | report secrecy | `runner.rs` — `scrub_detail`/`scrub_token` (blob:/file/HTTP/drive/UNC/abs paths, controls, 480-scalar/48-token char-boundary cap); `report.rs` — XML 1.0 illegal-char filtering | `runner::tests::scrubber_*`, `report::tests::serializer_checks_*` (quote/amp/NUL/Unicode/blob-in-token/Win+Unix paths, JSON round-trip) |
| M7-REWORK-F10 | no blanket clippy allow | `boa_fapi_wpt/src/lib.rs` — crate-wide `allow(clippy::expect_used)` removed; per-`#[cfg(test)]`-module allows only | `cargo clippy --workspace --all-targets --all-features -- -D warnings` green |
| M7-REWORK-F11 | CI/nightly wiring | `ci.yml` — job renamed M7 validation, M7 order comment, full-manifest strict + threads-2 + JSON report check, artifacts after generation; `nightly.yml` — Ubuntu nightly+miri bounded lib run, fixed-seed fuzz with timeout, Windows compatible-only note, explicit SKIP reasons | CI Ubuntu + Windows (awaiting owner verification); nightly schedule |

## M7-RW2 — rework-2 findings F12–F17 (`docs/reviews/M7-rework-2.md`)

| ID | Fixed boundary | Code | Test |
|---|---|---|---|
| M7-REWORK-2-F12 | hard timeout for default CLI | `main.rs` — every CLI file execution (threads 1 included) via `run_file_isolated` with wall kill; `run_file` stays library-only mapping | boundary: full strict exit 0 via isolated children (38 passed); threads 1/2 identical SHA-256 JSON |
| M7-REWORK-2-F13 | typed worker protocol | `main.rs` — `WORKER-OK` (verified row) / `WORKER-TIMEOUT` (reserved) / `WORKER-ERROR register\|prelude\|readback\|file-eval\|protocol`; kill/crash/overflow/corruption → `TIMEOUT`; register/prelude/readback → FAIL rows via gate; single-line/UTF-8/size/ID verification | boundary: `--worker-file 99` → exit 2 index range; corrupt output → TIMEOUT |
| M7-REWORK-2-F14 | duplicate JSON keys rejected | `manifest.rs` — recursive `DuplicateJsonKey(key)` (root/source/files/subtests; key name only) | `manifest::tests::rejects_duplicate_json_keys_recursive` (root/source/subtest, mixed types) |
| M7-REWORK-2-F15 | embedded URL/path scrubbing | `runner.rs` — `scrub_token` matches `blob:`/`file:`/HTTP(S)/drive/UNC/abs-Unix anywhere in token, punctuation-preserving, host-kept HTTP(S) | `runner::tests::scrubber_redacts_paths_and_controls`, `report::tests::serializer_checks_*` (url=, parens, drive-comma, UNC-parens, 480-scalar Unicode, controls, round-trip) |
| M7-REWORK-2-F16 | exact repository identity | `manifest.rs` — exact canonical URL equality (prefix-match retired) | `manifest::tests::rejects_malicious_repository_and_bad_subtest_collision` (wpt-malicious/path/query/fragment/userinfo/http/slash + canonical valid) |
| M7-REWORK-2-F17 | subtest collision banned in file | `manifest.rs` — per-file `seen_names`, `DuplicateSubtest` (variant A, protocol unchanged) | same test: in-file collision → error, cross-file reuse → ok; stock manifest loads |
